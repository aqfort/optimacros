# Optimacros

## Interview assignment

### The task is in PDF file

Command line argumrnts are in `settings.json`

```;
"cmake.debugConfig": {
    "args": [
        "input.txt",
        "\"?ad\""
    ]
}
```
